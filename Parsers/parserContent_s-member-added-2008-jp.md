#### Parser Content
```Java
{
Name = s-member-added-2008-jp
    Vendor = Microsoft
    Product = Microsoft Windows
    Lms = Splunk
    DataType = "windows-member-added"
    TimeFormat = "MM/dd/yyyy hh:mm:ss a"
    Conditions = [ """exabeam_raw""", "EventCode=", "??????????????????????????????" ]
    Fields = [ """exabeam_raw=({time}\d+/\d+/\d+ \d+:\d+:\d+ (am|AM|pm|PM))""",
      """ComputerName=({host}[\w.\-]+)""",
      """EventCode=({event_code}[\w]+)""",
      """??????????????????????????????({group_type}[^\s]+) ??????????????????????????????????????????????????????""",
      """??????????????????:.+???????????????????:\s+({user}[^\s]+)""",
      """??????????????? ????????????:\s+({user_domain}[^\s]+)""",
      """???????????? ID:\s+({logon_id}[^\s]+)\s+""",
      """????????????:\s+?????????????????? ID:\s+({account_id}(?=[^\\]+\\)({sid_domain}[^\\]+)\\({sid_user}.+?)|(?:.+?))\s+??????????????????:""",
      """????????????:.+???????????????????:\s*({account_dn}.+?)\s*????????????:""",
      """????????????:\s+?????????????????? ID:\s+({group_id}[^\s]+)""",
      """????????????:.+????????????????:\s+({group_name}.+?)?\s+???????????? ????????????:""",
      """????????????:.+????????????? ????????????:\s+({group_domain}[^\s]+)"""]
    DupFields = [ "host->computer_name" ]
  }
```