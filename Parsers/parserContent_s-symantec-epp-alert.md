#### Parser Content
```Java
{
Name = s-symantec-epp-alert
    Vendor = Symantec
    Product = Symantec Endpoint Protection
    Lms = Splunk
    DataType = "alert"
    TimeFormat = "yyyy-MM-dd HH:mm:ss"
    Conditions = [ """????????????????????????????????????""", """,???????????? ID:""" ]
    Fields = [
      """exabeam_host=([^=]+@\s*)?({host}\S+)""",
      """({time}\d\d\d\d\-\d\d\-\d\d \d\d:\d\d:\d\d),???????????? ID:""",
      """???????????? ID:\s*({alert_id}\d+)""",
      """???????????? 1:\s*(SYSTEM|({user}[^\s,]+))""",
      """IP ????????????:\s*({src_ip}[A-Fa-f:\d.]+)""",
      """??????????????????:\s*({src_host}[\w\-.]+)""",
      """,({alert_name}[^,]+),???????????? ??????:""",
      """????????????:\s*({malware_url}[^,]+)""",
      """??????:\s*({threat_num}[^,]+)""",
      """??????:\s*({infection_num}[^,]+)""",
    ]
    DupFields = [ "alert_name->alert_type" ]
  }
```